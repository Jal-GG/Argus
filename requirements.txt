# Core Deep Learning & Computer Vision
# Python 3.10-3.12 recommended (3.8 is EOL)
torch>=2.2.0
torchvision>=0.17.0
opencv-python>=4.9.0
numpy>=1.26.0
scipy>=1.12.0          # Hungarian association for BYTE tracker

# Object Detection & Tracking
ultralytics>=8.2.0     # YOLO11/12 + built-in ByteTrack/BoT-SORT reference configs
# deep-sort-realtime removed: in-repo BYTE two-stage tracker (src/core/tracking)

# Person Re-Identification (optional extras)
# torchreid            # OSNet embedder; enable when GPU workflow is needed
# open_clip_torch      # CLIP-ReID style embeddings

# Image Processing
Pillow>=10.2.0

# Web Framework & API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9
jinja2>=3.1.3

# Database (SQLite default; swap psycopg2 for PostgreSQL deployments)
sqlalchemy>=2.0.25

# Configuration
pyyaml>=6.0.1
python-dotenv>=1.0.1
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Alerts & Notifications (optional channels)
requests>=2.31.0       # webhooks
# twilio>=8.10.0       # SMS
# python-telegram-bot>=21.0

# Utilities
tqdm>=4.66.0
psutil>=5.9.8

# Monitoring (optional; /metrics degrades to JSON without it)
prometheus-client>=0.20.0

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
httpx>=0.27.0          # FastAPI test client

# Development Tools
ruff>=0.3.0            # lint + format (replaces black/flake8/isort)
mypy>=1.8.0

# Optional: GPU acceleration / edge export
# tensorrt             # requires separate install
# onnx>=1.15.0
# onnxruntime-gpu>=1.17.0
